services:
  assignment_1_flask_app:
    build:
      context: .
      dockerfile: Dockerfile
  nginx:
    image: nginx:latest
    ports:
    - "80:80"
    volumes:
    - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
    - assignment_1_flask_app